# DECIDE
predicted_las_dirpath: ${oc.env:PREDICTED_LAS_DIRPATH}
inspection_shapefile_name: "inspection_{subset}.shp"
# "${oc.env:DATASET_DIR}/logs/runs/${oc.env:INSPECTION_SHAPEFILE_FOR_TUNING}/${inspection.inspection_shapefile_name}"
update_las: false  # memory intensive


# OPTIMIZE
comparison_shapefile_path: "${oc.env:INSPECTION_SHAPEFILE_FOR_OPTIMIZATION}"
metric_pair_to_maximize: none  # override when running with:
# +metric_pair_to_maximize=[CONFIRMATION_OF_REFUTATION,CONFIRMATION_ACCURACY]
# or
# +metric_pair_to_maximize=[PROPORTION_OF_REFUTATION,REFUTATION_ACCURACY]

# Comparison paramers
# could be increased to demand higher proba  of being building for confirmation
min_confidence_confirmation: 0.530670228606621
# could be augmented to demand higher proba of not being building for refutation
min_confidence_refutation: 0.8883092588939281
min_frac_confirmation: 0.8038408386802507
min_frac_refutation: 0.529331992372645

hparams_opti_output_csv_path: "module_hparams_opti.csv"