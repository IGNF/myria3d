# @package _global_
defaults:
  - RandLaNetDebugFineTune.yaml
  - override /datamodule/dataset_description/classification: SwissSURFACE3D_Building_class

model:
    # d_in: 9
    num_classes: 2  # should match /datamodule/dataset_description/classification

callbacks:
  finetune:
    unfreeze_decoder_train_epoch: 10

logger:
  comet:
    experiment_name: "RandLaNetDebugFineTuneBinaryClassification"

